import Title from "../components/titleSection";

const NotFound = ({ error }) => {
  return (
    <Title heading="404" subheading="Sorry we could not find that page"/>
  );
}
 
export default NotFound;